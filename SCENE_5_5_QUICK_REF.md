# SCENE 5.5 快速参考

## 核心 Label

```ren'py
label scene_5_5_rescue:
    # 潜入洪府的主场景
    # - 夜间背景 + 心跳音乐
    # - 两个主要选择（压低身形/大摇大摆）
    # - 分支导向不同结果

label scene_5_5_stealth_game:
    # 潜行小游戏主循环
    # - 随机生成目标方向
    # - 调用 stealth_minigame screen
    # - 处理结果（正确/错误/开挂）

label scene_5_5_stealth_success:
    # 潜行成功后的救援场景
    # - 救出兰中玉
    # - 进行温情场景
    # - 跳转主线
```

## 核心 Screen

```ren'py
screen stealth_minigame():
    # 游戏界面
    # - 显示步数进度
    # - 显示风险等级
    # - 提供 3 向选择按钮
    # - 提供一键开挂按钮
    
    # 返回值：
    # - "left" / "forward" / "right"（方向选择）
    # - "cheat"（使用开挂）
```

## 核心变量

```python
stealth_correct_steps      # 已正确的步数（0-3）
stealth_target_direction   # 当前目标方向
ooc_checkpoint             # 保存的 OOC 值
```

## OOC 变化表

| 操作 | OOC | 说明 |
|------|-----|------|
| 压低身形（选项 A） | -5 | 稳定反派人设 |
| 大摇大摆（选项 B） | +30 | 人设暴露 |
| 潜行选错 | +30 | 家丁警报 |
| 使用开挂 | -50 | 用技能换安全 |

## 流程

```
forged_document_success
    ↓
scene_5_5_rescue (选择路线)
    ├─→ 选项 A (压低身形，-5 OOC)
    │   ↓
    │   scene_5_5_stealth_game (潜行小游戏)
    │   ├─ [正确 3 次 or 开挂] → success ✅
    │   └─ [选错时] → OOC+30 → 重来或 Game Over ❌
    │
    └─→ 选项 B (大摇大摆，+30 OOC)
        ├─ [OOC >= 100] → game_over_ooc ❌
        └─ [OOC < 100] → 被发现 → 重新选择

scene_5_5_stealth_success (救出兰中玉)
    ↓
act2_night_stealth (继续主线)
```

## 游戏界面元素

### 方向选择
```
[← 左移]    [↑ 前进]    [右移 →]
 (墙角)      (屋顶)      (廊道)
```

### 开挂按钮
```
【一键开挂】使用分身术躲避巡逻
```

### 信息显示
- 已检测步数：X / 3
- 风险等级：▓▓▓▓▓ 极高 → ▓▓░░░ 可控

## 关键代码片段

### 调用潜行小游戏
```python
$ result = renpy.call_screen("stealth_minigame")

if result == stealth_target_direction:
    # 正确！
    $ stealth_correct_steps += 1
elif result == "cheat":
    # 开挂！
    $ update_ooc(-50)
    $ cheat_count += 1
else:
    # 错误！
    $ update_ooc(30)
```

### 显示 / 隐藏游戏界面
```python
show screen stealth_minigame      # 显示
hide screen stealth_minigame      # 隐藏
```

## 背景音乐

| 位置 | 音乐文件 | 播放 |
|------|---------|------|
| scene_5_5_rescue 期间 | heartbeat_tense.ogg | 心跳紧张 |
| 救援成功后 | quiet_night.ogg | 柔和温暖 |

## 屏幕特效

```python
with flash         # 闪白（选错时的警报）
with vpunch        # 垂直震屏（正确时的反馈）
with fade          # 淡入淡出（场景切换）
```

## 文件位置

| 内容 | 文件 | 行数 |
|------|------|------|
| scene_5_5_rescue label | script.rpy | ~280-350 |
| scene_5_5_stealth_game label | script.rpy | ~350-450 |
| scene_5_5_stealth_success label | script.rpy | ~450-520 |
| stealth_minigame screen | screens.rpy | ~1808-1920 |

---

**需要调整？直接修改相应文件即可！** 🎮
